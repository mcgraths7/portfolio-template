/**
 * Placeholder. This becomes the CMS-driven page: a Contentful `page` entry
 * carries an ordered list of sections, and a registry maps each section's
 * content type and `variant` onto a composition of Sorbet components.
 */
export default function Home() {
  return (
    <main>
      <h1>Portfolio template</h1>
      <p>Next.js is up. Sorbet is not wired in yet.</p>
    </main>
  );
}
